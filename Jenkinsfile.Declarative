pipeline {
  agent {
      kubernetes {
        label 'web-app'
        idleMinutes 5
        defaultContainer 'jnlp'
    }
  }
  stages {
    stage('Build') {
      steps {  // no container directive is needed as the maven container is the default
        echo "ola a todos"   
      }
    }
  }
}
