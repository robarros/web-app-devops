pipeline {
  agent {
      kubernetes {
        cloud 'k8s'
        label 'web-app'
        idleMinutes 3
        defaultContainer 'jnlp'
        yamlFile 'pods/KubernetesPod.yaml' 
    }
  }
  stages {
    stage('Build') {
      steps { 
        echo "ola a todos"   
      }
    }
    stage('config') {
      steps { 
         echo "rodando container"   
         container('alpine') {
           sh 'printenv'
        }
      }
    }
  }
}
