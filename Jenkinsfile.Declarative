pipeline {
  agent {
      kubernetes {
        cloud 'k8s'
        label 'web-app'
        idleMinutes 3
    }
  }
  stages {
    stage('Build') {
      steps {  // no container directive is needed as the maven container is the default
        echo "ola a todos"   
      }
    }
  }
}
