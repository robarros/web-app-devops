pipeline {
  agent {
      kubernetes {
        cloud 'k8s'
        label 'web-app'
        idleMinutes 3
        yamlFile 'pods/declarative_from_yaml_file/KubernetesPod.yaml' 
    }
  }
  stages {
    stage('Build') {
      steps {  // no container directive is needed as the maven container is the default
        echo "ola a todos"   
      }
    }
  }
}
