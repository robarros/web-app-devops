pipeline {
  agent {
      kubernetes {
        cloud 'k8s'
        idleMinutes '1'
        yamlFile 'pods/KubernetesPod.yaml' 
    }
  }
  stages {
    stage('Build') {
      steps { 
        echo "ola a todos"   
      }
    }
    stage('config') {
      steps { 
         echo "rodando container"   
         container('alpine') {
           sh 'printenv'
           sh 'ls -lha'
        }
      }
    }
  }
}
